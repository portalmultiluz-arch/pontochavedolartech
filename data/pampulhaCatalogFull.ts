import { Product } from '../types';

// Tabela e catálogo da Pampulha zerados a pedido do usuário (0 itens)
// Novos produtos serão adicionados exclusivamente via Upload de Arquivo (PDF/CSV) oficial
export const pampulhaCatalogFull: Product[] = [];
