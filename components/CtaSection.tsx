import React from 'react';

// Este componente não é mais usado ativamente, mas o arquivo é mantido
// com um componente válido para evitar erros de compilação em alguns ambientes.
const CtaSection = () => null;

export default CtaSection;
